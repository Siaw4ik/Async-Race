export type Car = {
  name: string;
  color: string;
  id: number;
};

export type State = {
  currentPage: number;
  totalCars: number;
};

export type Winner = {
  id: number;
  wins: number;
  time: number;
};
